PORT=3000

# Provider credentials (per-user connections are stored in the DB; these are the
# app-level service credentials used for syncing)
PSN_NPSSO=
STEAM_API_KEY=
STEAM_ID=
NINTENDO_SESSION_TOKEN=
RAWG_API_KEY=

# Auth
APP_URL=http://localhost:5173
GOOGLE_CLIENT_ID=
# 48+ random bytes, base64 (e.g. node -e "console.log(require('crypto').randomBytes(48).toString('base64'))")
JWT_SECRET=
# exactly 32 random bytes, base64 (e.g. node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
ENCRYPTION_KEY=
